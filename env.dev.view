# Local development environment (FastAPI only)
# Copy the values into your private .env if needed.

# Run API without bot
FASTAPI_ONLY=1

# Redis (recommended for 2FA secrets storage)
REDIS_URL=redis://localhost:6379/0

# WebApp
WEBAPP_QR_URL=http://127.0.0.1:8000
WEBAPP_ALLOWED_ORIGIN=http://localhost:3000
CSP_ALLOWED_ORIGIN=http://localhost:3000

# JWT for admin tokens (local dev)
JWT_ADMIN_SECRET_ACTIVE=dev_admin_secret
JWT_KID_ACTIVE=main

# Admin migration flags (keep disabled unless you test migrations)
ALLOW_ADMIN_MIGRATIONS=0
ADMIN_SQL_TOKEN=

# If you plan to test migrations, switch to PostgreSQL and enable flags above
# DATABASE_URL=postgresql://user:pass@localhost:5432/karma
