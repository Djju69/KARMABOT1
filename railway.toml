[build]
builder = "nixpacks"

[deploy]
startCommand = "sh -c 'uvicorn web.main:app --host 0.0.0.0 --port $PORT'"
healthcheckPath = "/health"
healthcheckTimeout = 300000

[env]
PYTHON_VERSION = "3.11"
DISABLE_POLLING = "1"
# DATABASE_URL = "sqlite:///app.db"
