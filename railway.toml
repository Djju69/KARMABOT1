[build]
builder = "nixpacks"

[deploy]
startCommand = "sh -c 'python -m uvicorn web.main:app --host 0.0.0.0 --port $PORT'"
healthcheckPath = "/health"
healthcheckTimeout = 300000

[env]
PYTHON_VERSION = "3.11"
DISABLE_POLLING = "1"
# PYTHON_VERSION can stay if already present
