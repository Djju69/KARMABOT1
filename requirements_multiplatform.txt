# === MULTI-PLATFORM SYSTEM DEPENDENCIES ===
# Только уникальные библиотеки для multiplatform
# fastapi и uvicorn берутся из основного requirements.txt

# === ВАЛИДАЦИЯ И ТИПИЗАЦИЯ ===
pydantic[email]==2.5.0
pydantic-settings==2.1.0
python-multipart==0.0.6

# === HTTP КЛИЕНТЫ ===
aiohttp==3.9.1
httpx==0.24.1
requests==2.31.0

# === ЛОГИРОВАНИЕ И МОНИТОРИНГ ===
structlog==23.2.0
prometheus-client==0.19.0
sentry-sdk[fastapi]==1.40.6
psutil==5.9.6

# === КЕШИРОВАНИЕ ===
redis==5.0.1
cachetools==5.3.2

# === ТЕСТИРОВАНИЕ ===
pytest==7.4.3
pytest-asyncio==0.21.1
pytest-mock==3.12.0

# === БЕЗОПАСНОСТЬ ===
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-jwt>=2.6.0
python-keycloak>=2.12.0
email-validator>=2.1.0

# === УТИЛИТЫ ===
python-dateutil==2.8.2
pytz==2023.3
uuid7==0.1.0
python-slugify>=8.0.1
python-magic>=0.4.27
jinja2>=3.1.2
pyotp==2.9.0
qrcode[pil]==7.4.2

# === ДОПОЛНИТЕЛЬНЫЕ ЗАВИСИМОСТИ ===
# Для работы с базами данных (если нужно)
# asyncpg==0.30.0  # PostgreSQL async driver
# psycopg2-binary==2.9.9  # PostgreSQL sync driver
# supabase==2.0.2  # Supabase client
# SQLAlchemy==2.0.23  # ORM
# aiosqlite>=0.19.0  # SQLite async driver
# alembic>=1.12.1  # Database migrations
